{
  "title": "Voorkeur",
  "$id": "https://commongateway.nl/klant.voorkeur.schema.json",
  "$schema": "https://docs.commongateway.nl/schemas/Entity.schema.json",
  "version": "0.0.1",
  "type": "object",
  "required": ["code", "naam"],
  "properties": {
    "code": {
      "title": "Code",
      "type": "string",
      "description": "De code, behorende bij de landnaam, opgenomen in de Landentabel van de GBA.",
      "minLength": 1,
      "required": true
    },
    "naam": {
      "title": "Naam",
      "type": "string",
      "description": "De naam van het land, zoals opgenomen in de Landentabel van de GBA.",
      "minLength": 1,
      "required": true
    }
  }
}